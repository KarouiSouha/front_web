# Example environment variables for Frontend
# Set this to your backend host (with or without trailing /api). The app will normalize it.
VITE_API_URL=https://fasi-backend.onrender.com/api

# In development you can use:
# VITE_API_URL=http://localhost:8000/api
